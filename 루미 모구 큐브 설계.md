# 루미 모구 큐브 설계

루미큐브(Rummikub)에서 모티브를 딴 타일 대전 게임. 손의 타일 14장을 런/그룹으로
등록해 **가장 먼저 다 내면 승리**. 조커는 「모구 어디가」의 모구 얼굴 이미지
(`game/assets/mogu-icon.png`)를 그대로 쓴 **모구 타일**. 모구 게임 시리즈의
열일곱 번째 게임, 단일 파일 `mogurummy.html`.

## 코어 규칙 (루미큐브 대응)

- **타일 106장**: 4색(빨강·파랑·주황·검정) × 1~13 × 2벌 + 모구 타일(조커) 2장
- **런** = 같은 색 연속 숫자 3장 이상 (1~13, 랩 없음) /
  **그룹** = 같은 숫자 다른 색 3~4장
- **모구 타일** = 아무 타일이나 대신함(빈칸 채움·양끝 연장·그룹 대체).
  게임이 끝날 때 손에 있으면 벌점 30점
- **첫 등록(개방)**: 자기 랙에서 낸 새 묶음들의 합계가 기준점(노말 30점) 이상이어야
  등록 시작. 개방 전에는 판 위 타일을 만질 수 없음
- 개방 후에는 판 위 타일을 **자유롭게 재배열**(묶음 분해·이동·조커 회수) 가능.
  단 턴 종료 시 모든 묶음이 유효해야 하고, 판 위 타일은 랙으로 못 가져옴
  (이번 턴에 자기가 낸 타일만 회수 가능)
- **뽑기/패스**: 낼 수 없거나 안 내면 더미에서 1장 뽑고 턴 종료(배치했던 턴은
  자동 되돌림). 더미가 비면 패스
- **승리**: 손 타일 소진. 더미 소진 후 전원 연속 패스 시 **쇼다운** — 손 합계가
  가장 적은 쪽 승리(동점이면 플레이어 우대)

## 조작 (탭 + 드래그)

- **탭**: 타일 선택 → 묶음/「+ 새 묶음」 탭으로 배치 (기존 방식 유지)
- **드래그**: 랙 타일을 끌어 판 위 묶음/빈 자리에 놓으면 바로 배치(빈 자리 = 새
  묶음). 판 위 타일도 드래그로 다른 묶음 이동·랙 회수(이번 턴에 낸 것만) 가능
- 드래그 중 드롭 대상(묶음·새 묶음·판)이 하늘색으로 하이라이트. 랙 안에서의
  드래그는 기존처럼 손패 정리. 규칙 검사·안내 토스트는 탭 로직을 재사용

## 컴퓨터 턴 연출

- 컴퓨터 턴은 약 5초(4.6~5.4초) 소요. 상단에 **모래시계**(위 모래 줄어들고
  아래로 떨어져 쌓이는 CSS 애니메이션, 턴 시간과 동기) + 상대 이름 표시

## 인원 2~4인 (선택식)

- 스테이지 선택 화면에서 **2인전/3인전/4인전** 선택, 저장됨
- 상대 AI: 🐭 치즈 → 🐹 햄냥 → 🐰 토토 순으로 인원만큼 등장
- AI는 개방 조합 탐색(값 큰 세트 우선 그리디) → 손 세트 내려놓기 → 기존 묶음에
  한 장씩 붙이기 순으로 플레이. 실력(skill)이 각 행동의 성공 확률을 결정

## 난이도 4단계 (이지 · 노말 · 하드 · 크레이지)

- 스테이지 선택 화면 상단에서 선택 (시리즈 공통 구성), 난이도별로 진행 독립 저장

| 난이도 | AI 실력 | 첫 등록 | AI 조커 활용 | 힌트 | 점수 |
|---|---|---|---|---|---|
| 이지 | 30% | 25점 | ✕ | 무제한 | ×0.8 |
| 노말 | 52% | 30점 | ○ | 3회 | ×1.0 |
| 하드 | 74% | 30점 | ○ | 1회 | ×1.5 |
| 크레이지 | 92% | 30점 | ○ | 0회 | ×2.2 |

- AI 실력은 스테이지에 따라 +0.7%p/스테이지 추가 상승 (최대 97%)

## 스테이지 (50스테이지, 선택·해금식)

- 50칸 그리드, 클리어 시 다음 해금. **5의 배수 = ★쥐왕 보스전**: 마지막 상대가
  👹 쥐왕으로 교체 — 실력 +12%p, 13장으로 시작(한 장 우위), 클리어 점수 ×1.5
- 점수 = (상대 남은 타일 합계 + 라운드 보너스) × 난이도 배율
- localStorage `mogurummy.v1`: `{diff, players, prog:{난이도별 {unlocked, cleared,
  bestScore}}}` 자동 저장 + 세이브 파일 내보내기/불러오기/초기화

## 조작 (터치 우선)

- 타일 **탭 = 집기**, 묶음/「+ 새 묶음」/랙 **탭 = 놓기**, 같은 타일 재탭 = 해제
- **손패 드래그 정리**: 랙 타일을 드래그하면 고스트가 따라오고 실시간으로 순서가
  바뀜(가로·줄바꿈 배치 인식). 드래그 직후의 탭은 선택으로 오인하지 않음.
  상대 턴에도 정리 가능
- 유효하지 않은 묶음은 빨간 테두리로 실시간 표시, 이번 턴에 낸 타일은 파란 광채
- 버튼: 🔢 숫자 정리(숫자→색)/🎨 색깔 정리(색→숫자, 상대 턴에도 가능) ·
  ↩ 되돌리기(턴 시작 상태로) · 💡 힌트(개방 조합/새 세트/붙일 곳 하이라이트) ·
  타일 뽑기(패스) · 턴 종료 ✔
- 상단 HUD: 스테이지·라운드·더미 수 · 상대 칩(남은 장수, 차례 표시, 개방 시 초록)

## 기술 구성

- 의존성 없는 단일 파일, DOM 기반 렌더(캔버스 미사용) — 저사양·모바일 친화
- 세트 판정은 순서 무관(`validSet`): 그룹 검사 + 런 경계 계산(조커 필요 수·양끝
  여유 검증). 표시는 `arrangeSet`으로 자동 정렬(조커를 빈칸 위치에)
- 첫 등록 잠금은 상호작용 단계에서 구조적으로 강제(잠긴 묶음 드롭/선택 차단)
- 런처 카드 17번째(`index.html`), 시리즈 공통 스타일(난이도 버튼·스테이지 그리드)

## 검증

- Node에서 DOM을 목킹해 헤드리스 구동 (`window.__RUMMY_TEST__` 훅):
  세트 판정 16종(그룹/런/조커 빈칸·양끝·랩 금지·중복 금지), 세트 점수,
  개방 조합 탐색, 난이도·인원 UI와 저장, 14장 배분·더미 수, 첫 등록 미달 거부→
  되돌리기→개방 성공, 개방 전 잠금, 타일 소진 승리→난이도별 해금 저장,
  AI 선소진 패배, 쇼다운(패스 순환→최소 합계 승리), 전 난이도×2/3/4인
  자동 완주(종료 보장), 보스 스테이지(쥐왕 13장), 손패 드래그(고스트·순서 이동·
  탭 억제)와 숫자/색깔 정렬·상대 턴 정리 확인 — 70개 체크 통과
