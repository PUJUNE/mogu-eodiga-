# 모구레이스 게임 설계

모구가 오픈탑 레이서를 몰고 해안 절벽 고속도로를 질주하는 아웃런식 레이싱.
조작을 마우스 하나로 몰아넣은 것이 이 게임의 중심이고, 모구 게임 시리즈
런처의 스물네 번째 게임으로 들어간다.

## 조작 — 마우스 전용 (이 게임의 코어)

주행 준비 화면에서 **클릭한 자리가 기준점**으로 잡히고, 이후 모든 조작은 그
기준점 대비 절대 위치로 해석된다. 손을 멈추면 그 입력이 그대로 유지되는
구조라서, 실제 차의 스로틀 페달·핸들을 손 하나로 겹쳐 쥔 감각에 가깝다.
기준점은 위로 밀 여유와 좌우 여유가 남도록 화면 상단 34%·좌우 18% 안쪽으로
클램프한다 — 화면 끝에 잡히면 엑셀을 끝까지 열 수 없기 때문이다.

| 입력 | 해석 |
|---|---|
| 기준점보다 **앞(위)** | 엑셀 — 멀수록 깊게 (화면 높이 30% 지점에서 전개) |
| 기준점 주변 **데드존** (화면 높이 3.5% 반경의 작은 원) | 페달 오프 — 엑셀도 브레이크도 밟지 않아 구름저항으로 자연 감속 |
| 기준점보다 **뒤(아래)** | 브레이크 — 멀수록 깊게 (화면 높이 20% 지점에서 풀 브레이크) |
| 마우스 **정지** | 직전 입력 그대로 유지 |
| 기준점 대비 **좌우** 이동 | 조향 — 화면 폭 28% 지점에서 최대 타각 |

브레이크 감속은 관성 감속에서 시작해 깊이에 따라 풀 브레이크로 보간한다 —
깊이×최대감속으로 하면 얕은 브레이크가 구름저항보다 약해서 "페달을 밟았는데
더 늦게 서는" 모순이 나고, 데드존 경계에서 감속이 불연속으로 튄다.

기준점은 위(엑셀 30%)·아래(브레이크 20%) 여유가 남도록 화면 34~78% 세로 구간
안으로 클램프한다. HUD에 기준점 십자선과 현재 커서 점을 항상 그리고, 커서 점은
엑셀 중 초록·브레이크 중 빨강·데드존에서 노랑으로 바뀐다. 커서가 창을 벗어나면
브레이크는 즉시 풀리고 엑셀은 서서히 닫힌다.

터치 환경에서는 첫 접촉점이 기준점이 되고 드래그가 같은 매핑을 따른다 —
아래로 당기면 브레이크이므로 별도 브레이크 버튼을 두지 않는다.

## 변속 모드 (오토 / 스틱 6단)

타이틀 화면에서 선택하고 저장에 기억된다. 두 모드는 같은 기어 물리를 쓰고
오토는 변속만 자동이라, 잘 모는 스틱과 오토의 페이스가 같다.

- 기어 6단 — 단마다 도달 상한(최고 속도의 17·31·47·63·81·100%)과 견인 배수
  (1.9→0.85)가 있어 저단은 세게 당기고 일찍 한계가 온다
- rpm = 현재 기어 상한 대비 속도. 레드라인(90%~)에서 토크가 테이퍼되고 100%에서
  리미터, 고단 저회전에서는 약하게 붙는다(럭). 저단 과속(다운시프트 직후)은
  오버레브로 엔진 브레이크가 걸린다
- 변속 직후 0.16초 토크 컷 (다운시프트는 절반)
- **오토** — rpm 93%에서 업, 34%에서 다운 (히스테리시스). HUD는 RPM 창만
- **스틱** — 휠 위/아래 또는 좌클릭 ▲/우클릭 ▼로 순차 변속, 화면 좌측 H패턴
  게이트(H-H 모양) 버튼이나 Q A W S E D 키(각각 1~6단)로 원하는 단 직결 변속.
  단을 건너뛸 수 있고, 급다운시프트는 오버레브 엔진 브레이크로 이어진다. HUD에 기어
  숫자 + RPM 창. 터치는 우측 ▲▼ 버튼
- 엔진음은 실제 rpm에서 음정을 만들어 변속하면 음이 뚝 떨어졌다 다시 차오른다

## 코어 루프 (한 판 약 60~110초, 체크포인트 주파)

1. **출발** — 제한시간이 흐르기 시작하고 커서 위치가 기준점으로 고정된다.
2. **주행** — 엑셀을 열어 속도를 올리고 커브에서 원심력에 밀리지 않게 조향한다.
   도로를 벗어나면 급감속하고, 가드레일에 닿으면 튕겨 나온다.
3. **추월** — 일반 차량은 순위 경쟁 상대가 아니라 장애물이다. 뒤에서 들이받으면
   속도가 깎이므로 차선을 미리 잡는 것이 빠르다.
4. **체크포인트** — 통과하면 제한시간이 연장된다. 남은 시간이 0이 되면 그 자리에서
   주행 종료.
5. **완주 판정** — 코스 끝에 도달하면 클리어. 남은 시간이 많을수록 별이 올라간다
   (제한시간의 15% 이상 ★2, 30% 이상 ★3).

## 스테이지 (90판 = 15월드 × 6)

- 코스가 길어지고(3,870→7,119세그먼트) 커브 곡률·교통량·언덕 기복이 같이 올라간다
- 커브는 원심력 계수와 물려 있어 곡률이 곧 통과 가능 속도가 된다. 최대 커브에서
  버틸 수 있는 속도가 1코스 72%에서 90코스 49%까지 내려간다
- 월드 테마: 1 해안 절벽 / 2 협곡 / 3 사막 고속도로 / 4 설산 고개 / 5 도시 야경 /
  6 노을 해협 / 7 새벽 협곡 / 8 황혼 사막 / 9 오로라 설원 / 10 자정 도심 /
  11 폭풍 해안 / 12 붉은 바위 캐니언 / 13 모래폭풍 사막 / 14 눈보라 고개 / 15 네온 시티.
  테마 6~15는 배경 사진 5장을 재활용하되 색조 틴트(노을·새벽·눈보라 등)로 분위기를 바꾼다
- 6의 배수 판은 **라이벌 구간**: 교통량이 한 단계 더 붙는다
- **제한시간은 실측으로 굽는다** — 숙련 봇을 시간제한 없이 달리게 해 체크포인트
  구간별 실제 소요를 재고, 여유율(1코스 55% → 90코스 25%)을 곱해 표로 박아 넣는다
  (`test/tune.mjs`). 물리·트랙 상수를 바꾸면 반드시 다시 구워야 한다
- 시드 = 스테이지 번호(결정적 생성) — 모구 어디가·모구 스키점프와 동일 원칙

## 난이도 모드 (모구 어디가 문법)

- 타이틀에서 이지/노말/하드/크레이지 선택, 저장에 기억. 노말이 아니면 HUD에 표기
- 트랙 형상·차량 물리는 그대로 두고 **제한시간 배율(timeMul)과 교통량 배율(trafficMul)만**
  조절한다 — 형상을 건드리면 실측으로 구운 제한시간 표가 어긋난다
- 이지 1.3×/0.6× · 노말 1.0/1.0 · 하드 0.92×/1.3× · 크레이지 0.85×/1.7×.
  timeMul은 여유율(25~55%)이 곱해진 표에 다시 곱해지므로 크레이지도 완주 가능선을
  지킨다 (sim-test가 전 난이도 봇 완주를 검증)
- 별점·기록은 난이도 공용 (어디가와 동일)

## 저장·진행

- localStorage `mogurace-save-v1`: 스테이지별 별점·최고 기록(완주 시각) + 변속 모드·난이도
- 코스 맵(90칸 그리드)에서 선택, ★1 이상이면 다음 코스 해금
- 실패해도 즉시 재도전 (짧은 런 구조)
- **세이브 내보내기/불러오기** (어디가 문법) — 타이틀에서 JSON 파일로 내려받고,
  불러올 때는 코스별 높은 별점·빠른 기록만 취하는 병합(merge)이라 진행을 잃지 않는다

## 기술 구성

- **2D 캔버스 유사3D** — 도로를 z 방향 세그먼트 배열로 두고 원근 투영으로
  사다리꼴을 쌓는 아웃런 계열 방식. three.js 미사용
- **화풍은 실사로 통일한다.** 모구 시리즈가 원래 실사 고양이 사진에 그린 요소를
  얹어 왔으므로 배경 사진·다이캐스트 미니카·실사 모구가 한 톤으로 떨어진다
  - 배경: 월드별 실사 사진 한 장을 지평선(화면 중앙)에 맞춰 흘린다. 사진은 이어붙는
    그림이 아니라서 좌우 반전 타일링으로 이음매를 없앤다
  - 노면: 절차적 사다리꼴 위에 실사 아스팔트 결을 덧입힌다. 패턴이 원근을 따르지
    않으므로 가까울수록 진한 띠로 나눠 얹어 근사한다
  - 교통 차량: 후방 눈높이 뷰를 직접 그린다(세단·밴·트럭 × 6색, 야간은 미등 점등).
    다이캐스트 사진 팩(24방향)은 전 프레임이 위에서 내려찍은 각도라 추격 시점에
    붙이면 탑다운 차를 세워 둔 판처럼 보여 쓰지 못했다. 옆을 스치는 차는 원본을
    몇 배로 늘려야 해 화면 폭 34%로 크기를 묶고 그보다 가까워지면 그리지 않는다
  - 교통 차량은 차선 중앙(-2/3·0·+2/3)에 정렬해 제 차선을 지키며 달린다 — 표류 없음
  - 도색은 편도 3차선: 왼쪽 가장자리 황색 실선 + 오른쪽 흰 실선 + ±1/3 흰 점선.
    중앙 황색 복선은 가운데 차선을 반으로 갈라 보여 두지 않는다
  - 노변은 인공물(가로등·표지)만 둔다. 실사 배경 위의 벡터 초목은 화풍이 충돌한다
  - 월드 지면색은 테마색과 사진 지평선 아래 실제 색을 섞어 배경과 이어지게 맞춘다
- 외부 에셋의 출처·라이선스는 `mogurace/CREDITS.txt`에 정리하고, 표기 의무가 있는
  CC-BY 항목은 타이틀 화면에도 노출한다. 로드뷰 서비스(구글·네이버·카카오) 이미지는
  약관상 저장·재배포가 막혀 있어 쓰지 않고, share-alike가 붙는 Mapillary·KartaView도 제외
- 모듈: `mogurace/src/` rng / levels(트랙·교통·테마) / logic(차량 물리·마우스
  입력 해석·충돌·체크포인트, DOM 무의존) / render(2D 캔버스) / audio / ui / main
- 물리: 엑셀·브레이크·구름저항의 속도 적분, 커브 원심력이 차를 바깥으로 밀고,
  노면 이탈 시 접지 계수 하락. 모구 스프라이트는 `mogu-ski.png`(뒷모습) 재사용
- **보이는 폭 = 부딪히는 폭.** 차폭은 `logic.js`의 `CAR_HALF`·`PLAYER_HALF`(도로 반폭 기준)
  하나만 두고 렌더가 그 값을 그대로 투영해 그린다. 두 값을 따로 두면 비어 보이는데 부딪히는
  상태가 된다. 3차선이라 차선 간격이 0.667이고 차폭 합이 그보다 작아야 옆 차선 차와 스치지 않는다
- 충돌은 전진 뒤에 스윕으로 판정한다. 고정 폭 창으로 보면 프레임 도약이 차 길이의 2배를
  넘을 때 앞차를 통째로 지나친다 (실주행은 dt를 DT_CAP(1/36)로 묶어 도약 500 < 임계 520이지만,
  logic은 임의 dt로 호출될 수 있어 방어함)
- `mogurace/build.py` → 단일 `mogurace.html`, 런처 카드 마지막에 추가

## 검증

- `test/level-test.mjs` — 90스테이지 트랙 형상·연속성·커브 상한·결정성
- `test/sim-test.mjs` — 마우스 매핑 경계값과 봇 주행 완주 가능성(90/90), 난이도 실효성
- `test/browser-test.mjs` — 실제 브라우저에서 클릭으로 기준점을 잡고 마우스로 주행해
  체크포인트까지 통과시키는 흐름 + 빌드본·터치·콘솔 에러 0건
- 주행 정책은 `test/shim.mjs`의 `botControl` 하나만 쓴다 — sim-test와 tune이 정책을
  각자 들고 있으면 굽힌 제한시간 표가 실제 주행과 어긋난다
