# 슈퍼모구 게임 설계

횡스크롤 점프 액션(플랫포머) 장르 문법을 모구 테마로 만든 게임. 모구가 쥐·새
악당을 밟고 블록을 치며 깃발까지 달린다. 모구 게임 시리즈의 열한 번째 게임.
레벨·그래픽·사운드는 전부 시드 기반 오리지널 생성.

## 코어 규칙 (플랫포머 장르 문법)

- **이동**: ←→ 달리기(가속·마찰), B(Shift) 대시, A(Space) 점프 — 누르는 길이로
  점프 높이 조절, 낙하 중 발밑 적을 **밟으면 처치**하고 튕겨 오름
- **블록**: ?블록(아래서 치면 코인·파워업), 벽돌(슈퍼 상태만 파괴), 토관 장애물
- **파워업 3단계**: 꼬마 모구 → 츄르(슈퍼 모구, 커짐) → 캣닢(털뭉치 발사).
  피격 시 한 단계 하락 + 2초 무적, 꼬마 상태 피격 = 사망. 반짝 방울 = 8초 무적
- **악당**: 걷는 쥐(밟으면 납작) / 새(사인 곡선 비행) / 고슴도치(밟으면 웅크림 →
  차면 미끄러지며 아군 무기가 되지만 잘못 닿으면 피해)
- **낙사·용암·시간 초과** = 사망 → 무제한 재도전(스테이지 처음부터)
- **골인**: 깃발 — 잡은 높이 비례 보너스 + 남은 시간 × 10. 성 스테이지는 보스
  쥐마왕을 3번 밟아야 클리어
- 별점: ★1 클리어 / ★2 코인 60% 이상 / ★3 코인 60% + 노피격

## 스테이지 (8월드 × 4 = 32스테이지)

- 월드 테마: 초원 → 사막 → 숲 → 바다 절벽 → 설원 → 동굴 → 하늘 → 마왕성 (색·배경 변화)
- 각 월드 1~3 = 필드(길이·갭·적 밀도 상승), 4 = 성(용암 갭 + 보스전)
- 시드 = 스테이지 번호, 결정적 생성: 지형 세그먼트(평지/갭/언덕/토관/벽돌열/
  계단) 연쇄 + 적·블록 배치. 갭 폭은 점프 가능 상한(4타일) 보장
- 저장: `supermogu-save-v1` 최고 도달·별점, 이어하기/처음부터/시리즈 복귀

## 조작

- 키보드: ←→ 이동, Space/Z 점프(길게 = 높이), Shift/X 대시·털뭉치 발사, ESC 일시정지, R 재시작
- 터치: 좌하 ◀▶, 우하 Ⓐ 점프(큰 버튼)·Ⓑ 대시/발사, 우상 ⏸

## 기술 구성

- 2D Canvas 480×270(고해상도 버퍼), 16px 타일, 카메라 횡스크롤
- 모구: 사진 얼굴 + 코드 몸통(꼬마/슈퍼 크기 차이), 무적 시 점멸
- 모듈: `supermogu/src/` rng / levels(타일 생성) / logic(물리·판정, DOM 무의존) /
  render / audio / ui / main — 시리즈 공통 구조
- `supermogu/build.py` → 단일 `supermogu.html`, 런처 카드 11번째

## 검증

1. `level-test.mjs` — 32스테이지: 시작 안전지대·깃발/보스 존재·갭 폭 ≤ 점프
   상한·계단 정합·결정성 전수
2. `sim-test.mjs` — 이동 가속·마찰, 가변 점프(짧게 vs 길게), 타일 충돌(벽·천장·
   바닥), 밟기 처치·튕김, 접촉 피해(단계 하락→사망), ?블록 파워업(성장·발사),
   벽돌 파괴(슈퍼만), 고슴도치 셸 슬라이드, 낙사·시간 초과, 깃발 클리어·보너스,
   보스 3밟기, 결정성
3. `browser-test.mjs` — 런처 11카드, 키보드·터치 조작, 스크린샷
