# 모구맨 게임 설계

스노우 브라더스(Snow Bros., 1990 토아플랜) 방식의 고정 화면 플랫폼 액션을 모구
테마로 만든 게임. 눈 대신 고양이 털을 발사해 적을 털뭉치로 감싸고, 털뭉치를
발로 차서 굴려 다른 적을 쓸어버리는 연쇄가 재미의 중심.

## 시리즈 구조

- 배포 링크(https://pujune.github.io/mogu-eodiga-/)의 첫 화면은 **모구 게임 시리즈
  런처**가 되고, 모구 어디가 / 모구맨 중 골라서 시작함
- 모구 어디가는 `eodiga.html`, 모구맨은 `moguman.html`로 접근 (기존 `모구 어디가.html`
  더블클릭 실행도 유지)
- 세이브는 게임별 별도 localStorage 키 (같은 도메인이라 경로 변경 영향 없음)

## 코어 규칙 (스노우 브라더스 대응)

| 스노우 브라더스 | 모구맨 |
| --- | --- |
| 눈 발사 | 고양이 털 발사 (뭉게뭉게 털 탄환, 사거리 약 100px) |
| 눈 맞은 적이 점점 눈사람 | 털 맞은 적이 점점 털에 감김 (탄 수: 쥐·새 3~4, 청소기 7) |
| 완성된 눈덩이를 차면 구름 | 완성된 털뭉치를 몸으로 밀면 굴러감 |
| 구르는 눈덩이가 적을 쓸어버림 | 구르는 털뭉치가 벽에 튕기며 적을 쓸어버림 (벽 4회 반사 후 소멸) |
| 연쇄 킬 보너스 | 연쇄 킬마다 점수 배가 + 츄르 드롭 |
| 방치하면 눈이 풀림 | 2.6초마다 털이 한 단계 풀림, 털뭉치도 7초 방치 시 화나서 부활 |
| 시간 초과 시 적 분노 | 50초 경과 시 남은 적 전원 분노(1.5배속·붉은 기) |

- 부분 감김(털 1 이상) 상태의 적은 버둥거리며 정지, 접촉 무해
- 맨몸 적·보스 접촉 시 목숨 1 감소, 시작 지점 부활(2.5초 무적), 목숨 3
- 게임 오버 시 무제한 컨티뉴(해당 스테이지 재시작, 점수 유지)

## 조작

- 키보드: ←→ 이동, ↑/Z 점프, Space/X 털 발사, ESC 일시정지, R 재시작
- 터치(모바일): 좌하 ◀▶, 우하 B(발사)·A(점프), 우상 ⏸ — 모구 어디가와 같은
  가상 패드 문법, 기존 키 부울 매핑(물리 불변)

## 스테이지 (50판 = 5월드 × 10)

- 고정 화면 아레나(논리 해상도 320×240, 정수 배율 확대·픽셀 스타일),
  양쪽 벽 + 바닥 + 한 방향(아래→위) 통과 플랫폼 3~4열
- 시드 = 스테이지 번호(결정적 생성). 모든 플랫폼이 바닥에서 도달 가능하도록
  생성 시 아래 열과의 겹침을 강제하고, 검증기(BFS)로 50판 전수 확인
- 월드 테마: 1 모구네 거실 / 2 앞마당 / 3 골목길 / 4 지붕 위 / 5 꿈속
- 적 투입: W1 생쥐 → W2 +참새(비행, 털 맞으면 추락) → W3 +점프 쥐 →
  W4 +로봇청소기(빠르고 털 7탄) → W5 전 종 혼합
- 10·20·30·40·50판은 보스전: 왕생쥐 / 심술 까마귀 / 폭주 청소기 / 그림자 고양이 /
  쥐마왕. 보스는 털 명중당 HP 감소(털뭉치 명중은 3), 주기적으로 부하 소환,
  HP 0이면 거대 털뭉치가 되어 굴러 나가며 클리어

## 점수·저장

- 적 처치 100, 굴린 털뭉치 연쇄 킬 500×연쇄, 츄르 500, 생선(희귀) 목숨 +1
- 저장(localStorage `moguman-save-v1`): 최고 도달 스테이지, 최고 점수
- 타이틀에서 이어하기(도달 스테이지부터) / 처음부터 선택

## 기술 구성

- 2D Canvas, 외부 라이브러리 없음(three.js 불필요). 모구 스프라이트는 기존
  `game/assets/mogu-icon.png` 재사용(base64 내장), 적·탄·털뭉치는 코드 드로잉
- 모듈: `moguman/src/` rng / levels(생성) / logic(물리·AI, DOM 무의존) /
  render(캔버스) / audio(WebAudio 합성) / ui(HUD·화면·저장) / main(루프·입력)
- logic을 렌더와 분리해 node 단독 헤드리스 시뮬레이션 테스트 가능
- `moguman/build.py`로 단일 `moguman.html` 병합 (모구 어디가와 같은 방식)

## 검증

1. `level-test.mjs` — 50판 전수: 플랫폼 경계·겹침, 바닥에서 전 플랫폼 도달
   가능(BFS), 적 스폰 유효, 보스판 구성
2. `sim-test.mjs` — 헤드리스 시뮬레이션: 낙하 착지 → 털 명중 → 털뭉치 완성 →
   차기 → 굴러서 연쇄 킬 → 스테이지 클리어, 피격·부활, 보스 HP 감소
3. 플레이라이트 — 타이틀→플레이→클리어 흐름, 터치 패드 13종 검사, 런처에서
   두 게임 진입, 스크린샷 실물 확인
